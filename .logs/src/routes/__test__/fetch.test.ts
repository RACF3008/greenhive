import request from 'supertest';
import mongoose from 'mongoose';

import { app } from '../../app';
import { Log } from '../../models/log';

const createDevice = async () => {};

const createLog = async () => {};

it('can fetch a list of logs', async () => {
  await createDevice();
});
